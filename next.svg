import Link from "next/link";
import { Listing } from "@/types";

interface ListingCardProps {
  listing: Listing;
}

const ListingCard = ({ listing }: ListingCardProps) => {
  const placeholderColors: Record<string, string> = {
    "1": "from-blue-400 to-cyan-300",
    "2": "from-gray-400 to-slate-300",
    "3": "from-orange-400 to-amber-300",
    "4": "from-green-400 to-emerald-300",
    "5": "from-yellow-400 to-lime-300",
    "6": "from-red-400 to-rose-300",
    "7": "from-teal-400 to-cyan-300",
    "8": "from-purple-400 to-indigo-300",
  };

  const gradient = placeholderColors[listing.id] || "from-gray-300 to-gray-200";

  return (
    <Link href={`/rooms/${listing.id}`} className="group block">
      {/* Photo placeholder */}
      <div className={`relative w-full aspect-square rounded-2xl overflow-hidden bg-gradient-to-br ${gradient} mb-3`}>
        <div className="absolute inset-0 flex items-center justify-center">
          <span className="text-white text-5xl opacity-60">🏠</span>
        </div>
        {/* Heart button */}
        <button
          onClick={(e) => e.preventDefault()}
          className="absolute top-3 right-3 w-8 h-8 flex items-center justify-center rounded-full bg-white bg-opacity-80 hover:bg-opacity-100 transition shadow-sm"
        >
          <svg className="w-4 h-4 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path
              strokeLinecap="round"
              strokeLinejoin="round"
              strokeWidth={2}
              d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"
            />
          </svg>
        </button>
        {/* Category badge */}
        <div className="absolute bottom-3 left-3">
          <span className="bg-white bg-opacity-90 text-gray-700 text-xs font-medium px-2 py-1 rounded-full">
            {listing.category}
          </span>
        </div>
      </div>

      {/* Info */}
      <div className="space-y-1">
        <div className="flex items-start justify-between gap-2">
          <h3 className="text-sm font-semibold text-gray-900 line-clamp-1 group-hover:text-rose-500 transition">
            {listing.title}
          </h3>
          <div className="flex items-center gap-0.5 flex-shrink-0">
            <svg className="w-3.5 h-3.5 text-gray-800 fill-current" viewBox="0 0 20 20">
              <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
            </svg>
            <span className="text-xs font-medium text-gray-800">{listing.rating}</span>
          </div>
        </div>
        <p className="text-sm text-gray-500">{listing.location}</p>
        <p className="text-sm text-gray-500">{listing.reviewCount} reseñas</p>
        <p className="text-sm font-semibold text-gray-900">
          <span className="text-base">{listing.price}€</span>{" "}
          <span className="font-normal text-gray-500">noche</span>
        </p>
      </div>
    </Link>
  );
};

export default ListingCard;
